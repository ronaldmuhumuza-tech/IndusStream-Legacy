## V4 Validation Summary

V4 validates an edge-to-cloud telemetry workflow using Raspberry Pi, MQTT, AWS IoT Core, Lambda, DynamoDB, S3, SNS and GitHub Actions.

The dashboard implementation is deferred to IndusStream v5, where it will be treated as part of the Linux edge runtime and observability workstream.

Validation evidence is captured under `docs/v4-validation/`.